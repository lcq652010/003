# 这是一个测试配置文件
# 用于测试 PropertiesParser 的功能

# 数据库配置
db.host=localhost
db.port=3306
db.name=testdb
db.username=admin
db.password=password123

# 服务器配置
server.host=127.0.0.1
server.port=8080
server.timeout=30

# 以下是测试错误情况
# 重复的键
db.host=anotherhost

# 空值
empty.key=

# 无等号的行
invalid line without equals

# 只有等号没有键
=value without key

# 正常配置项
app.name=TestApp
app.version=1.0.0
app.debug=true
